<h2>Search for recipes everyone can have:</h2>

<%= render partial: "./nav" %>

<div class="left-of-sidebar">
  <h3>Party: <%= link_to @party.name %></h3>

  <div id='query_form'>
    <%= form_tag({controller: "parties", action: "query_results"}, method: "get") do %>
      <p><%= label_tag :query, "What do you want to cook?" %></p>
      <p><%= text_field_tag :query, nil, placeholder: "Examples: Onion Soup, Chicken, etc." %></p>
      <p><%= submit_tag("Search for recipes") %></p>
    <% end %>
  </div>
</div>

<div class="sidebar">
  Can't render sidebar on this page yet because @upcoming_parties is undefined for this route.
  You can delete this div and replace it with a render when it gets fixed.
</div>